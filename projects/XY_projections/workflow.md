# XY Projections — workflow notes

## Layers

| var name               | id                                               | name                  | source                                          | provider      | CRS        |
|------------------------|--------------------------------------------------|-----------------------|-------------------------------------------------|---------------|------------|
| `high_schools_2227`    | `high_schools_2227`                              | High Schools 2227     | `data/high_schools_2227.shp` (generated)        | ogr           | EPSG:2227  |
| `high_schools`         | `high_schools`                                   | High Schools          | `data/high_schools.csv?type=csv&…`              | delimitedtext | EPSG:4326  |
| `libraries`            | `Libraries_53815974_27c8_4c61_ac6e_25a82f907635` | Libraries             | `data/Libraries.shp`                            | ogr           | EPSG:2227  |
| `paloalto_cityboundary`| `PaloAlto_cityboundary_95ec1801_…`               | PaloAlto_cityboundary | `data/PaloAlto_CityBoundary.shp`                | ogr           | EPSG:2227  |
| `carto_test_3`         | `Carto_test_3_9e1d3db0_…`                        | Carto test 3          | XYZ tile service                                | wms           | —          |
| `osm_gray_scale`       | `OSM_gray_scale_ab6c302d_…`                      | OSM gray scale        | XYZ tile service                                | wms           | —          |

## Processing steps

| layer              | depends on    | action                                         | output                       |
|--------------------|---------------|------------------------------------------------|------------------------------|
| `high_schools_2227`| `high_schools`| geopandas: read CSV, reproject to EPSG:2227    | `data/high_schools_2227.shp` |

## Notes

- Project CRS: EPSG:2227 (NAD83 State Plane California Zone III, US feet).
- `high_schools`: delimited text CSV, Lat/Lon WGS84 (EPSG:4326), 2 Palo Alto
  public high schools. Source layer for the reprojection step. Requires
  `geometry_type="Point"` so `_inject_layers` includes it in the output QGS.
- `high_schools_2227`: generated by `make build` via `_reproject()` in
  `layers/high_schools_2227.py`. Reads the CSV with pandas, builds a GeoDataFrame
  with `gpd.points_from_xy`, then `.to_crs("EPSG:2227").to_file(output)`.
  Skipped on rebuild if `data/high_schools_2227.shp` already exists.
- `libraries` style comes from `styles/libraries.xml` (extracted from the QGZ).
  Layer IDs for QGZ-imported layers are preserved as QGIS UUIDs; new layers use
  human-readable IDs.
